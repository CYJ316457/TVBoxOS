# tvboxos 项目附加规则

- 每次修改任意文件后，必须立即重新打开该文件，检查一遍中文是否出现乱码。
- 对包含中文的源码、脚本、JSON、Markdown、YAML、配置文件和文档，必须确认内容可正常显示，且不存在 `锟斤拷`、`??`、替换字符 `U+FFFD`、明显乱码或异常编码痕迹。
- 如果发现中文乱码，必须先修复编码问题并再次复检，通过后才能继续后续修改、构建、提交或交付。
